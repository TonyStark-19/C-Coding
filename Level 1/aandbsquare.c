#include <stdio.h>
// This code is to find the value of (a+b)^2
int main()
{
    int a, b, c;

    printf("Enter value of a : ");
    scanf("%d", &a);
    printf("Enter value of b : ");
    scanf("%d", &b);

    printf("The Value of (a+b)^2 is : %d ", c = (a + b) * (a + b));
}