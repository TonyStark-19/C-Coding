#include <stdio.h>
// This is program to find average of 3 numbers
int main()
{
    int a, b, c, avg;

    a = 100;
    b = 200;
    c = 300;

    avg = (a + b + c) / 3;

    printf("Average is : %d", avg);
}